//
//  UpdatePasswordViewModel.swift
//  Laza
//
//  Created by Dimas Wisodewo on 29/07/23.
//

import Foundation

class UpdatePasswordViewModel {
    
    func updatePassword(newPassword: String) {
        
//        guard let savedData = DataPersistentManager.shared.getUserDataFromUserDefaults() else { return }
//        var user = savedData.0
//
//        let newSalt = HashingManager.shared.getSalt()
//        let newHashedPassword = HashingManager.shared.stringToHash(string: newPassword, salt: newSalt)
//        let password = Password(hashedPassword: newHashedPassword, salt: newSalt)
//
//        user.password = newHashedPassword
//
//        // Save updated data to UserDefaults
//        DataPersistentManager.shared.saveUserDataToUserDefaults(user: user, password: password, isLoggedIn: true)
    }
}
